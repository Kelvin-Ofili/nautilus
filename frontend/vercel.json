{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "regions": ["iad1"],
  "env": {
    "VITE_API_URL": {
      "description": "Backend API URL (e.g., https://nautilus-production.up.railway.app)"
    }
  }
}
